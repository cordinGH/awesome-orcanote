---
description: 如何查询历史对话的说明。当需要查看历史对话时使用。
---

## 对话块

块类型为`aichat`的块即是对话块。

## 如何查询历史对话

调用`query_blocks`工具查询同时满足以下条件的块：

- 块类型为`aichat`

- 文本包含 [关键词]

查询结果为含有关键词的历史对话块，调用`get_blocks_text`工具即可获得历史对话的摘要。
